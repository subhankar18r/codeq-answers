#include <stdio.h>
int main()
{
    // Declaration and Initialization
    int arr1D[5] = {1, 2, 3, 4, 5};

    // Accessing elements
    for (int i = 0; i < 5; i++)
    {
        printf("%d ", arr1D[i]);
    }
    return 0;
}